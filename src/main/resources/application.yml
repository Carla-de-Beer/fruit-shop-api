logging:
  level:
    org:
      springframework: ERROR

management:
  endpoint:
    beans:
      enabled: true
    metrics:
      enabled: true
    health:
      showDetails: "always"
  server:
    port: 9005
    servlet:
      contextPath: "/api/v1/categories/"
  endpoints:
    web:
      exposure:
        include: "*"
server:
  port: 8080
